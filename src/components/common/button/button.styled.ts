import styled from 'styled-components';

export const ButtonStyled = styled.button`
  outline: none;
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
  border: none;
  background: inherit;
  color: inherit;
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  letter-spacing: inherit;
  text-align: inherit;
  cursor: pointer;
  
  &:active, &:focus {
    outline: none;
    border: none;
  }
`;


